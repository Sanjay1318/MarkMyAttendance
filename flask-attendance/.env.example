# Copy this to .env and fill in your values
# Flask
SECRET_KEY=change-me-to-a-long-random-string-32chars

# Database — MySQL
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=attendance_db

# Set to 0 in production
FLASK_DEBUG=1
EOF
